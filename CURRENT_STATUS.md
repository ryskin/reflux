# REFLUX - Текущий статус

## ✅ Что работает (Sprint 1 Complete)

### 1. База данных
- ✅ PostgreSQL с Kysely ORM
- ✅ Таблицы: flows, runs, nodes, flow_versions
- ✅ Репозитории для всех операций
- ✅ Миграции

### 2. REST API (http://localhost:4000)
- ✅ `GET /api/flows` - Список flows
- ✅ `POST /api/flows` - Создание flow
- ✅ `GET /api/flows/:id` - Детали flow
- ✅ `GET /api/runs` - История запусков
- ✅ `GET /api/nodes` - Каталог нод
- ✅ `POST /api/nodes/register` - Регистрация ноды

### 3. UI (http://localhost:3002)
- ✅ Главная страница с инструкциями
- ✅ Список flows `/flows`
- ✅ Детали flow с визуализацией `/flows/:id`
- ✅ Создание flow `/flows/new`
- ✅ История запусков `/runs`
- ✅ Каталог нод `/nodes`
- ✅ React Flow canvas для визуализации

### 4. Зарегистрированные ноды
- ✅ `nodes.webhook.trigger` - Webhook триггер
- ✅ `nodes.http.request` - HTTP запросы
- ✅ `nodes.transform.execute` - JavaScript обработка
- ✅ `test.node` - Тестовая нода

### 5. Документация
- ✅ Туториалы `/docs/tutorials/CREATE_YOUR_FIRST_FLOW.md`
- ✅ Quick start `/QUICK_START.md`
- ✅ Примеры `/examples/openai-chat-flow.md`
- ✅ Тест e2e `/test-e2e.sh`

---

## ❌ Что НЕ работает

### 1. Выполнение workflows
**Проблема**: Кнопка "Execute Flow" в UI зависает

**Причина**: Интеграция с Temporal для реального выполнения workflows еще не реализована.

**Код проблемы** (`packages/api/src/routes/flows.ts:118`):
```typescript
const client = await WorkflowClient.create(); // ❌ Этот класс не существует
const result = await client.executeWorkflow(flow.spec as any, inputs); // ❌ Не работает
```

**Что происходит**:
1. Вы нажимаете "Execute Flow"
2. UI отправляет `POST /api/flows/:id/execute`
3. API пытается создать `WorkflowClient`
4. Класс не найден → код зависает
5. UI показывает "Executing..." бесконечно

### 2. Temporal Worker
**Проблема**: Worker для выполнения нод не запущен

**Что нужно**:
- Temporal Server (запускается через Docker)
- Worker процесс, который слушает задачи
- Activities для каждой ноды (webhook, http, transform)

### 3. Реальное выполнение нод
Ноды зарегистрированы в каталоге, но:
- Нет кода для выполнения HTTP запросов
- Нет sandbox для JavaScript transform
- Нет webhook сервера для триггеров

---

## 🎯 Что делать сейчас

### Вариант 1: Просто изучать UI
Вы можете:
- ✅ Создавать flows через `/flows/new`
- ✅ Просматривать flows в визуальном редакторе
- ✅ Изучать структуру flows
- ✅ Смотреть каталог нод
- ❌ НО выполнять flows пока нельзя

### Вариант 2: Подождать Sprint 2
В Sprint 2 будет реализовано:
- Temporal Worker integration
- Реальное выполнение workflows
- Activities для каждой ноды
- Трейсинг и мониторинг
- S3 storage для артефактов

### Вариант 3: Использовать API напрямую
Можно создавать и просматривать flows через API:

```bash
# Создать flow
curl -X POST http://localhost:4000/api/flows \
  -H "Content-Type: application/json" \
  -d @/tmp/my-flow.json

# Посмотреть все flows
curl http://localhost:4000/api/flows | jq

# Посмотреть детали
curl http://localhost:4000/api/flows/[FLOW_ID] | jq
```

---

## 📋 Roadmap

### Sprint 1 ✅ (DONE)
- Database schema
- REST API
- UI with React Flow
- Node catalog
- Flow visualization

### Sprint 2 🚧 (NEXT)
- **Temporal Worker** - для выполнения workflows
- **Activities** - реальный код нод
- **Storage (MinIO)** - для артефактов
- **Tracing (ClickHouse)** - для метрик
- **Retry policies** - для надежности

### Sprint 3-8 📅 (LATER)
- Tabular data processing (Excel/CSV)
- Dynamic graph mutations
- Self-improvement (Critic/Optimizer)
- AI node generation
- Natural language workflows

---

## 🔧 Как починить Execute

Если хотите чтобы Execute заработал прямо сейчас, нужно:

### Быстрый фикс (mock)
Замените код в `/packages/api/src/routes/flows.ts:107-125` на:

```typescript
router.post('/:id/execute', async (req, res) => {
  try {
    const flow = await FlowRepository.getById(req.params.id);
    if (!flow) {
      return res.status(404).json({ error: 'Flow not found' });
    }

    const inputs = req.body.inputs || {};

    // TODO: Real execution via Temporal (Sprint 2)
    // For now, just create a mock run record
    const run = await RunRepository.create({
      flow_id: flow.id,
      flow_version: flow.version,
      status: 'completed', // Mock status
      inputs,
      outputs: { message: 'Mock execution - real execution coming in Sprint 2' },
      temporal_workflow_id: `mock-${Date.now()}`,
      temporal_run_id: `mock-${Date.now()}`,
    });

    res.json(run);
  } catch (error: any) {
    res.status(500).json({ error: error.message });
  }
});
```

Это создаст запись в базе, но не выполнит workflow реально.

### Полное решение (Sprint 2)
1. Установить и запустить Temporal Server
2. Создать Temporal Worker
3. Реализовать Activities для каждой ноды
4. Интегрировать с API

---

## 💡 Рекомендации

**Для изучения системы сейчас:**
1. Создавайте flows через UI
2. Смотрите как они визуализируются
3. Изучайте API через curl
4. Читайте документацию

**Если нужно реальное выполнение:**
- Ждите Sprint 2 (или помогите реализовать! 😊)

**Если хотите протестировать логику:**
- Используйте mock фикс выше
- Или тестируйте через unit tests

---

## ❓ FAQ

**Q: Почему Execute не работает?**
A: Temporal Worker integration еще не реализована (это Sprint 2)

**Q: Когда заработает выполнение?**
A: В Sprint 2 (следующий этап разработки)

**Q: Можно ли использовать систему сейчас?**
A: Да, для создания и визуализации flows. Но не для выполнения.

**Q: Что делать с зависанием Execute?**
A: Либо примените mock фикс, либо просто не используйте эту кнопку пока

---

## 🎉 Итог

**Sprint 1 полностью завершен** - все что должно было работать, работает:
- ✅ База данных
- ✅ API
- ✅ UI
- ✅ Визуализация
- ✅ Каталог нод

**Execute workflow** - это функционал Sprint 2, который еще предстоит реализовать.

Вы можете продолжать изучать систему, создавать flows, смотреть визуализацию, но выполнение пока недоступно.
